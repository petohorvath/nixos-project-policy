if ! has nix; then
  log_error "Install host Nix first; see docs/development.md."
  return 1
fi
if ! has use_flake; then
  log_error "Use a direnv version with flake support or load nix-direnv; see docs/development.md."
  return 1
fi
use flake
